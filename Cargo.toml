[package]
authors = ["Yunho Cho"]
name = "rbook-utils"
version = "0.0.1"
edition = "2024"
description = """
A high-level wrapper over `rbook` for easy ebook parsing/conversion/rendering
"""
license = "Apache-2.0"
repository = "https://github.com/yunho-c/rbook-utils"
homepage = "https://yunho-c.github.io/rbook"
documentation = "https://docs.rs/rbook-utils"
readme = "README.md"
categories = ["parser-implementations"]
keywords = ["ebook", "epub", "parser", "markdown", "cli"]

# Ignore these files and folders when packaging
exclude = [
    "assets/*",
    ".github/",
]

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
html2md = "0.2"
kuchiki = "0.8"
rbook = "0.7.5"
urlencoding = "2.1"
walkdir = "2.5"
regex = "1.11"
once_cell = "1.20"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha1 = "0.10"
